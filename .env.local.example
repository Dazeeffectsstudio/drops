# Copie ce fichier en .env.local (ignoré par git) et renseigne tes propres
# valeurs depuis le tableau de bord Supabase : Project Settings > API.

# URL du projet Supabase (Project Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Clé publique "anon" (Project Settings > API > anon public)
# Sans risque à exposer au navigateur : elle ne peut que lire les offres.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Clé "service role" (Project Settings > API > service_role secret)
# NE JAMAIS exposer au navigateur. Utilisée uniquement côté serveur par le
# dashboard /admin pour ajouter/modifier/supprimer des offres.
SUPABASE_SERVICE_ROLE_KEY=

# Optionnel — protège la route /api/sync (voir src/app/api/sync/route.ts).
# Sans cette valeur, /api/sync reste accessible sans secret. Une fois
# définie, tout appel doit inclure l'en-tête
# "Authorization: Bearer <valeur>". Choisis une chaîne longue et aléatoire.
SYNC_SECRET=

# Liste des emails autorisés à accéder à /admin et /admin/sync (séparés par
# des virgules, ex: "toi@example.com,associe@example.com"). IMPORTANT : dès
# que Supabase Auth est actif, /admin se ferme à tout le monde SAUF ces
# adresses — ajoute-toi ici avant de déployer, sinon tu seras bloqué·e hors
# de ton propre tableau de bord. Si Supabase n'est pas configuré du tout,
# /admin reste ouvert comme avant (développement local uniquement).
ADMIN_EMAILS=

# Pour activer la connexion Google/GitHub : dans le tableau de bord
# Supabase, va dans Authentication > Providers, active Google et/ou GitHub,
# et renseigne les identifiants OAuth créés depuis Google Cloud Console /
# GitHub Developer Settings (callback URL à donner : celle affichée par
# Supabase sur cette même page). La connexion par email/mot de passe
# fonctionne sans aucune configuration supplémentaire. Les boutons
# Google/GitHub se masquent automatiquement tant que le provider
# correspondant n'est pas activé côté Supabase (voir src/lib/supabase/oauth-status.ts).

# Clé API Resend (https://resend.com — compte gratuit) pour l'envoi réel des
# emails de notification. Sans cette valeur, aucun email n'est envoyé : le
# service se contente d'écrire dans les logs du serveur (mode développement).
RESEND_API_KEY=
# Adresse d'expédition. "onboarding@resend.dev" fonctionne sans configurer
# de domaine, mais n'est utilisable qu'en test (Resend limite les
# destinataires). Pour un vrai lancement, vérifie ton propre domaine dans
# Resend puis utilise une adresse "DROPS <notifications@tondomaine.com>".
RESEND_FROM_EMAIL=DROPS <onboarding@resend.dev>

# Clés VAPID pour les notifications push navigateur (Web Push). Contrairement
# à Resend, ce ne sont PAS des identifiants d'un service tiers : c'est une
# simple paire de clés cryptographiques générée pour ce projet (déjà générée
# et renseignée dans le .env.local local — ne les régénère que si tu veux
# invalider tous les abonnements push existants).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT_EMAIL=mailto:toi@example.com

# URL publique de production (V9) — IMPORTANT à définir dès que le site a un
# vrai domaine : sans elle, le sitemap, robots.txt et les images Open Graph
# pointeraient vers localhost, et le site reste volontairement en "noindex"
# (invisible pour Google) tant qu'elle n'est pas configurée. Pas de "/" à la fin.
NEXT_PUBLIC_SITE_URL=https://drops.be

# Analytics (V9, optionnel) — laisse les deux vides pour ne rien envoyer du
# tout (aucun script chargé, aucune donnée collectée). Renseigne l'un OU
# l'autre (pas besoin des deux).
# Google Analytics 4 : Admin > Flux de données > ton flux Web > ID de mesure (commence par "G-").
NEXT_PUBLIC_GA_MEASUREMENT_ID=
# Plausible (alternative respectueuse de la vie privée, sans bannière cookie
# nécessaire) : le nom de domaine tel qu'ajouté sur plausible.io.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
